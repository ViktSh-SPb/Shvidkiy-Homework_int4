spring:
  application: name=Shvidkiy-Homework_int4
  datasource:
    url: jdbc:postgresql://localhost:10000/learn_db
    username: user
    password: user
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
      dialect: org.hibernate.dialect.PostgreSQL17Dialect
server:
  port: 10001
  error:
    whitelabel:
      enabled: false
